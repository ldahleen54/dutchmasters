import React from "react"
const Table = ({ ...props }) => {
    // let headers = props.headers.map((header) => {
    //     <th>{header}</th>
    // });
        return (
            <div className="table">
                <table>
                    <p>testing55</p>
                </table>
            </div>
        );
    }

export default Table;